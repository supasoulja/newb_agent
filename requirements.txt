# ── Core (required on all platforms) ──────────────────────────────────
pydantic>=2.0
sqlite-vec
psutil
fastapi
uvicorn[standard]
python-multipart
pytest
onnxruntime>=1.20
tokenizers
numpy
huggingface_hub

# ── Desktop app (optional — only needed for `python app.py`) ─────────
# On Linux these require system packages (GTK, libappindicator, etc.).
# If you only want the web UI (`python web.py`), skip these.
pywebview>=5.0; sys_platform == "win32"
pystray; sys_platform == "win32"
Pillow; sys_platform == "win32"
keyboard; sys_platform == "win32"
