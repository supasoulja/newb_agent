# Normalize all text files to LF in the repo.
# Git will convert to CRLF on checkout on Windows if core.autocrlf is true,
# but the stored version is always LF — keeps diffs clean cross-platform.
* text=auto eol=lf

# Explicitly binary — never mangle these
*.png binary
*.jpg binary
*.ico binary
*.db  binary
